# Three Dimensions Backend Configuration
# Copy this file to .env and fill in your values

# Server Configuration
PORT=3000
NODE_ENV=production

# CORS - Allowed origins (comma-separated)
# Change this to your actual domain in production
ALLOWED_ORIGINS=https://three-dimensions.de,https://www.three-dimensions.de

# Security
# IMPORTANT: Generate a strong random encryption key for production!
# Use: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY=your-secure-encryption-key-change-this

# Salt for IP address hashing (for privacy)
IP_HASH_SALT=your-random-salt-for-ip-hashing

# Data Retention (in days) - Default 180 (6 months) for GDPR compliance
RETENTION_DAYS=180

# Email Configuration (SMTP)
# Your SMTP server settings
SMTP_HOST=smtp.your-email-provider.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@example.com
SMTP_PASS=your-email-password
SMTP_FROM=noreply@three-dimensions.de

# Notification Email - Where to send new inquiry notifications
NOTIFICATION_EMAIL=kontakt@three-dimensions.de

# Auto-Response - Set to 'true' to send automatic confirmation emails to customers
SEND_AUTO_RESPONSE=true

# Data Directory - Where to store encrypted submission data
# Default: ./data (relative to backend folder)
# DATA_DIR=/path/to/secure/data/folder
